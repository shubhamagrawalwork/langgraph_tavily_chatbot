# LangGraph Tavily Chatbot

## Backend
FastAPI app in `backend.py` , deployed on Render

## Frontend
Streamlit app in `streamlit_app.py`

## Local run
```bash
uvicorn backend:app --reload
streamlit run streamlit_app.py
